interface:
  display_name: "notionx"
  short_description: "Build and maintain notionx projects"
  default_prompt: "Use $notionx to inspect this notionx project, follow the framework contracts, and make the requested change with appropriate validation."
policy:
  allow_implicit_invocation: true
