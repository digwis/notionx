---
"@notionx/core": patch
---

Update `@notionx/core` to use the canary-verified `vinext` dependency range and
remove the obsolete `vinext@0.1.1` workspace patch so installs and releases no
longer depend on the old patched build.
