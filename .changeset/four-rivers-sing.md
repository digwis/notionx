---
"@notionx/create-nextion-app": patch
---

Bump the scaffolded `vinext` and `@vinext/cloudflare` template versions to the
latest canary-verified range so new projects start from the dependency set that
already passed `digwis` typecheck, test, and build verification.
