---
"@notionx/skill": patch
---

Add first-class Codex skill directory installation, shared `.agents/skills`
support, explicit `codex-rules` installs for `AGENTS.md`, and bundled OpenAI
metadata while refreshing instructions for `@notionx/cli` and
`npm create notionx@latest`.
