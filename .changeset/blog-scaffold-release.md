---
"@notionx/create-nextion-app": minor
---

Improve the default blog scaffold for `create-nextion-app` by generating a minimal blog-oriented Notion schema, naming the provisioned database after the app, and seeding sample posts with page body content. This also fixes Notion seeding when the title property resolves to `Name` instead of `Title`.
