---
"@notionx/core": minor
---

Add typed structured page block runtime support for `hero`,
`feature-grid`, and `story`. The core page and Notion mapping layers now
preserve structured block fields so reusable blocks render through
dedicated typed components instead of relying on generic body content
alone.
