# {{projectName}} is a standalone project. It does not import from
# other monorepo packages directly — `@nextion/core` is fetched
# from GitHub Packages or, in the vinext-monorepo dev case, linked
# via `pnpm install --filter`.
packages: []
