// Re-export of @vinext/foundation/auth/session for backward compatibility.
// The starter used to ship its own copy; the canonical version now
// lives in the package.

export type { SessionUser } from "@vinext/foundation/auth/session";
